#!/usr/bin/env node

var program = require('commander');

program
    .version('0.0.1')
    .usage('[options]')
    .description('Wikipedia articles on command line')
    .option('-q, --query <search term>', 'Wikipedia article to search')
    .option('--log-filename <file>', 'Path to log queries')
    .parse(process.argv);
